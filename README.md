# shumee-website
http://shum.ee website

## Local development

```bash
npm install
npm run dev
```

The dev server (http://localhost:4173) serves the static site with caching disabled so changes show up immediately. Use `npm run sass:watch` in another terminal if you need to recompile styles from SCSS.
